# Adversarial audit: C200--C204

**Date:** 2026-09-06

**Subject:** one actual periodic viscous linear concentration endpoint,
including its physical normalization, retained Fourier projection and
complete linear trajectory budget.

## 1. Independence and scope

Three independent authors developed the smooth selectors (C200), compact
physical endpoint geometry (C201), and periodic viscous upper bridge
(C202). The root author derived the retained-band dual test (C203) and
assembled the actual-solution theorem (C204). Two independent adversarial
auditors checked the proofs, constants, domains and composition. The
component authors also independently derived the additional entrance
and trajectory budgets; the endpoint auditor checked those additions.

These are in-repository independent re-derivations. No external auditor
bundle, external referee approval, formally verified proof assistant
certificate, numerical nonlinear solution, or new external C185 source
clearance is claimed.

## 2. What was checked mathematically

**C200.** The auditor independently re-derived the complex covector
neighborhood, changing-frame one-block bounds, strict projective
contraction, holomorphic square-root normalization, exact finite-horizon
Kelvin formulas and multilinear Cauchy constants. Both original-base
spatial derivatives were checked, including the explicit promotion of
C194's third-flow-jet calculation. The replacement \(3000\) by \(2999\)
retains \(2999>e^8\), and all new derivative costs are polynomial in
the horizon. No invariant infinite-time bundle was inferred.

**C201.** The audit checked the rank-one return shear, the irrational
box/grid covering argument, the signed common-lattice Riesz lower bound,
the small parameter-symbol perturbation estimate and its real parity.
It checked every exact-curl derivative cost and the exact quadratic
energy balance, including \(1/300<\lambda<300\) and \(1/20<N<30\).
Coefficient norm was not substituted for physical norm.

**C202.** The auditor verified periodic lift-equivariance and injectivity
of the transported compact patch, then the actual torus pressure
problem. It re-derived the \(H^1\) growth bound, the heat-decaying pump
clock and the \(\sqrt{\mu s}\) viscous comparison. The general initial
symbol extension was checked separately: C197's projected constant
polarization estimate is not directly applicable to C200. The local
anisotropic parameter image, all initial mixed jets, the initial
exact-curl \(H^1\) constant and the factorial schedule were verified.

**C203.** The audit re-derived the sinc-fourth-power Fourier transform,
its positive inner-box transform, the moments \(54,81\), mode count
\(1000V\) and test norm \(64\). It checked periodization and unfolding
against the periodic nonlinear-phase field, including the global tail
outside the local ball and the negative reality partner. The
unprojected version follows directly from the same Hölder test; no
\(L^\infty\) contraction of Fourier projection is assumed.

**C204.** Both auditors checked the complete joint initial base/covector
tube, including the entire sine parameter image, and the actual
finite-scale mesh and physical clock. They checked the local
inverse-flow ball for C203, phase curvature, terminal symbol
derivatives, the global leading-field bound, center rounding and
negative-band separation. The curl, PDE, viscous and stable-ballast
errors are all charged to the same solution. The reality coefficient
\(\sqrt2\), physical normalization and final denominator \(2099520\)
were independently verified.

The final focused audit also cleared the entrance bound, the shared-peak
quotient \(>(25/24)q^{3/8}\), and the full-trajectory bounds
\[
 \sup_s\|v(s)\|_2<10^{16}b,\quad
 \int\|v\|_2^2ds<4\cdot10^{33}b^2,\quad
 \mu\int\|\nabla v\|_2^2ds<2\cdot10^{34}b^2.
\]

## 3. Corrections and scope preserved during audit

- The common C195 radius bound was not reused for a large covector
  aperture. Initial position and covector perturbations were separated:
  the latter costs \(J_1\), while the former costs \(J_1J_2\).
- The Euclidean radius of a parameter cube includes its \(\sqrt3\)
  factor; C204 uses \(400c/L>220\sqrt3\,c/L\).
- C200's selected initial data required a separately proved
  general-symbol version of C197 and a smaller parameter domain.
- The signed carriers were placed in \(d\mathbb Z^3\), so reality
  completion belongs to the same lattice for the Riesz proof.
- The exact-curl correction was included when comparing to C203's
  leading field. A small PDE error alone does not bound that correction.
- Nearest-integer endpoint-center rounding was charged by doubling the
  test widths. The band is actually projected; no exact support of the
  nonlinear-phase solution is presumed.
- The physical initial \(L^\infty\) bound is \(20000bq^{9/8}\).
  C182's stronger \(bq\) hypothesis is not asserted.
- The retained box has the required logarithmic orders but is centered
  at the C159 covector. C180's specific center, splitter and exit chart
  have not been identified with it.
- Finite carrier scale does not mean that a compact smooth envelope is
  bandlimited.
- Linearized energy equality does not imply nonlinear pump/wake
  closure. The uncancelled full nonlinear defect remains
  \(P(v\cdot\nabla v)\). Terminal singular-center tracking remains open,
  and the C185 operator-norm citation restriction remains in force.

## 4. Verification

The five new dependency-free checkers pass. They check exact rational
constants, derivative recurrences, matrix/convolution identities,
finite-scale monotonicity and exponent ledgers. The analytic proofs and
their independent re-derivations carry the general PDE and Fourier
arguments; checker passage is not presented as their substitute.

All **113** repository checkers pass in the final complete sweep.
The five new checker modules compile. Claim IDs are unique, referenced
local artifacts exist, and the final files pass control-character,
math-delimiter and whitespace checks. Two delimiter defects introduced
during document editing were repaired; they did not affect the
mathematical statements or checker code.
