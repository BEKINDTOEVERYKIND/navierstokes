# Adversarial audit: C195--C196 and the success portfolio

**Date:** 2026-08-30

**Scope:** C195 off-ray \(A_2\) finite-horizon dominated cones, C196 periodic endpoint/phase-space
claim, the counterfactual success portfolio, and the narrow maintenance
patch to engines/return_map_opt.py

## 1. C195 attacks and repairs

### Attack A: the zero-level Kelvin formula was reused off level

Away from \(f=0\), the horizontal block \(S\) is trace free but not
symmetric. Direct moving-frame differentiation gives the missing

\[
               B_{21}\text{ term }=
               \frac{m(t\cdot Sp-p\cdot St)}{QD}.
\]

An exact rational nonsymmetric-matrix rederivation was added. A separate
exact \(A_2\) root-frame calculation proves

\[
                    t\cdot Sp-p\cdot St=2\sqrt3 fD.
\]

The interval certificate now carries \(|f|\le10^{-11}\). The earlier
zero-level shortcut is not used.

### Attack B: nearby base points were transported with one derivative too few

The draft used an \(L^2\) radius ledger. On one common Euclidean lift of the
initial torus neighborhood the repaired proof defines
\(r=\max\{\|\delta X_0\|_2,\|\delta k_0\|_2\}\). Comparing covectors launched
from distinct base points pays the base derivative \(J_1\) before the
Eulerian covector derivative \(J_2\). The repaired bound is

\[
 |\delta k(t)|<434{,}000{,}000(1+t)^3r,
\]

and the certified criterion is

\[
 r(1+TR_{\rm filt})^3\le1/(8.7\cdot10^{13}).
\]

The resulting \(q=n^8\) thresholds are \(n\ge10^{10}\),
\(n\ge10^{39}\), and \(n\ge10^8\) for the three stated radii. The checker
raises the exact integer inequalities to common denominators.

### Attack C: the proposed projector first-jet bound was not a theorem

The draft charged several section-frame derivatives by unexplained coarse
factors. That entire claim and checker function were deleted. C195 is
explicitly a finite-horizon \(C^0\) dominated-cone theorem; cone-field and
selected-line derivatives remain open. It selects no canonical or invariant
stable/unstable lines.

### Attack C.2: displayed sentinels did not prove the general block identity

The nonsymmetric rational substitutions detected the missing off-level
term, but by themselves did not prove the four displayed formulas for a
general trace-free horizontal block. The final checker independently
differentiates both moving-frame vectors in an exact multivariate rational
ring and verifies all four entries after denominator clearing and
coefficient collection. The rational substitutions remain only as
additional sentinels.

### C195 rerun

The complete dependency-free outward calculation was rerun after all
repairs. It returned

* forward lower endpoint \(>3009.757266\);
* reflected-inverse lower endpoint \(>3005.135765\);
* forward image contained in
  \([0.1405737,0.1898012]\);
* inverse image contained in
  \([0.1402257,0.1903237]\);
* every positive forward-cone line and sign-reflected backward-cone line
  have angle sine at least \(520/569\);
* the associated two-line oblique projector norms are at most \(569/520\);
  and
* each block's projective slope contraction is below
  \(1/4{,}500{,}000\).

The continuous one-block bound \(3^{55}\) follows directly from
\(\|L_{\rm Kelvin}\|\le18\), \(T<76/25\), and \(e<3\). No periodic phase,
finite-frequency, viscous, or nonlinear consequence is inferred.

**C195 audit verdict:** pass at the corrected finite-horizon \(C^0\)
dominated-cone scope. No invariant bundle, canonical \(E^u,E^s\), or
persistence statement is included.

## 2. C196 attacks and repairs

### Attack D: a projective tube was counted as three narrowed coordinates

A projective angular \(q^{-1/4}\) tube has two transverse widths
\(q^{3/4}\) and, in a fixed relative annulus, a full radial width \(q\).
It therefore has \(q^{5/2}\), not \(q^{9/4}\), modes and concentration
ceiling \(q^{5/4}\), not \(q^{9/8}\). The repaired claim distinguishes:

* a three-coordinate relative cube: \(q^{9/4}\) modes and \(q^{9/8}\)
  ceiling;
* a projective tube: \(q^{5/2}\) modes and \(q^{5/4}\) ceiling; and
* C193's one-envelope band: \(q^{3/4}\) modes and \(q^{3/8}\)
  concentration.

C195 controls finite-horizon cone directions and does not automatically impose the radial
restriction. The checker now verifies all three exponents separately.

### Attack E: one C194 beam was silently promoted to \(q^2\) beams

C194 is a compactly supported \(\mathbb R^3\) one-beam theorem on
\(|f|\le1/10\). C196's Dirichlet field is global, periodic, and has
\(M\ge q^2/13824\) carriers. The normalized triangle majorant contains
\(\sqrt M\), hence a full power of \(q\), which destroys the \(37/600\)
and \(14/75\)
first-term margins.

Section 3 is now explicitly conditional on an unproved \(M\)-uniform
Fourier-integral almost-orthogonality, spatial-annulus localization, and
band/tail theorem. C196 does not assert that its endpoint profiles satisfy
C194.

### Attack F: the bridge majorant was stated as necessity

The broad-stable C194 estimate certifies decay only for \(g<25/57\); it
does not prove failure beyond that value. The collision with C182's
principal fixed-band entrance exponent \(g\ge1/2\) is now stated only as a
collision of the two certified sufficient windows. The omitted second-term
margins were added:

\[
                 \widetilde\mu_-=\frac{29}{200},\qquad
                 \widetilde\mu_+=\frac{13}{25}.
\]

### Attack G: hidden constants in the full-aperture construction

The construction was replaced by the explicit cofinal family
\(q=m^{24}\), \(L_+=m^8\), \(L_-=m^2\), with carrier grid (1.16). It gives

\[
 M\ge q^2/13824,\quad p_{\min}\ge10q,\quad
 \alpha_+\le(\sqrt3/10)q^{-2/3},\quad
 \alpha_-\le(\sqrt3/10)q^{-11/12},
\]

and the explicit concentration constant \(c_*\) in C196 (1.18). The text
also corrects “curl error”: the curl is exact; \(\alpha\) measures the
sideband/polarization correction.

### Attack H: endpoint-support scope and reality constant

A variable-coefficient evolution can populate new Fourier modes. Every
ceiling is now attached to the endpoint component actually retained or
projected in the stated support. C180's \(8\delta^{3/2}\) constant is for
one oriented box; a disjoint reality completion costs at most \(\sqrt2\).

The exact real/divergence-free construction, focus identity, Parseval norm
bound, \(q^{9/8}\) and \(q^{3/2}\) powers, \(64\delta^3q^3/J^4\) count,
\(J^{-2}\) and \(J^{-1/2}\) taxes, and all rational clock margins were
independently re-derived and confirmed.

**C196 audit verdict:** pass as an exact kinematic construction, exact
support ceiling, and explicitly conditional one-beam arithmetic. No common
dynamic witness is claimed.

## 3. Portfolio and engine audit

The strategy note withdraws globally exhaustive “one path left” language
and pre-registers four scoped experiments. Its similarity algebra was
checked:

* \(\mu'=(q/g)\mu\) and the axisymmetric \(G,\eta\) exit scalings are exact;
* the Type-II corridor has \(\alpha+\beta=1\), with finite-energy boundary
  \(\beta\ge2/5\) and Euler-dominant boundary \(\beta<1/2\);
* at \((\alpha,\beta)=(11/20,9/20)\), viscosity is
  \(\nu e^{-\tau/10}\); and
* the centered-ball flux coefficient is \(5\beta/2-1=1/8\).

Source scope is explicit: Constantin--Ignatova--Vicol and Chae--Wolf
supply the stated direct guardrails; Pineau--Vicol is methodology for a
different parabolic rotated problem.

### Attack I: an infinite-energy inner profile was too close to promotion

The \(|y|^{-11/9}\) large-similarity-radius law is compatible with finite
local terminal energy, but the corresponding profile is not in
\(L^2(\mathbb R^3)\).  It is now labeled only as an inner-profile discovery
ansatz.  Even an interval-certified weighted residual, the \(1/8\) flux
identity, and a finite bad spectrum do not produce Clay-admissible data.

Promotion is separately conditioned on a certified time-dependent
finite-energy cutoff or outer matching, the complete induced
matching-annulus defect, and explicit control of pressure/Biot--Savart
coupling between the inner and outer regions.  In the unforced route the
defect must cancel in the actual dynamics rather than be declared as a
force.  The standing terminal singular-center local-energy and
critical-norm obligations also remain.  A weighted inner residual alone is
therefore recorded as discovery evidence, not progress toward a Clay
singular solution.

Four implementation defects were repaired in engines/return_map_opt.py:

1. the seed-dependent time step is no longer detached by .item();
2. vector \(L^p\), concentration, and replay metrics use \(|u|\), computed
   from \(|u|^2\) without a float32-underflowing square-root epsilon;
3. the historic componentwise anti-cheat caps are translated
   conservatively by \(1/\sqrt3\) and \(1/3\); and
4. score, field, normalization, and shift are snapshotted atomically at the
   best pre-step iterate across restarts.

The historical independent engines/qp/audit_qp.py is left unchanged so its
landed numerical record remains reproducible. PyTorch is not installed in
this container, so only AST/bytecode compilation was possible; no runtime
or gradient smoke result is claimed. The engine is still a selected-shell
optimizer and is not represented as the full-wake search.

**Portfolio/engine audit verdict:** strategy scope passes after the
inner/outer promotion boundary above; engine patch is syntax-checked only,
with the exact portability limitation recorded.
